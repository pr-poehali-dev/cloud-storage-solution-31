{
  "tests": [
    {
      "name": "Webhook ping (unknown event)",
      "method": "POST",
      "path": "/",
      "body": {"event": "payment.waiting_for_capture", "object": {}},
      "expectedStatus": 200,
      "bodyMatcher": "partial"
    },
    {
      "name": "GET returns 200 (cron via root)",
      "method": "GET",
      "path": "/",
      "expectedStatus": 200,
      "bodyMatcher": "partial"
    }
  ]
}